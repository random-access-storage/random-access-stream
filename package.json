{
  "name": "random-access-stream",
  "description": "Consume a random-access storage as a readable stream",
  "version": "2.0.0",
  "repository": "randoma-acces-storage/random-access-stream",
  "license": "MIT",
  "scripts": {
    "test": "tape test.js && standard"
  },
  "devDependencies": {
    "random-access-memory": "^3.0.0",
    "tape": "^4.9.0"
  },
  "dependencies": {
    "from2": "^2.3.0"
  }
}
